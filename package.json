{
  "name": "virtualdesk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:agent": "bash scripts/build-agent.sh",
    "dev:ui": "npm --prefix src-ui run dev",
    "build:ui": "npm --prefix src-ui run build",
    "tauri:dev": "npm run build:agent && npm --prefix src-ui run build && npm --prefix src-tauri run tauri dev",
    "tauri:build": "npm run build:agent && npm --prefix src-ui run build && npm --prefix src-tauri run tauri build"
  }
}
